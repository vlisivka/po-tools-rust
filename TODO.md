# TODO - po-tools

## Загальні завдання
- [x] Локалізація програми українською мовою
- [ ] Оптимізація роботи: Розбиття великих PO файлів на менші частини (chunks) (src/main.rs:138)
- [ ] Підтримка багаторядкових/однорядкових повідомлень (`multiline/singleline`) (src/main.rs:143)

## Парсер (`src/parser.rs`)
- [x] **Fuzzy Messages**: Парсинг коментарів для підтримки повідомлень позначених як "fuzzy" (src/main.rs:135, src/parser.rs:357, src/parser.rs:452)
- [ ] **Context Extraction**: Парсинг коментарів для отримання посилань на вихідний код (напр. `#: file.rs:123`) та додавання відповідних фрагментів коду в контекст для ШІ.
- [ ] Налаштування форматування: зробити змінну `multiline` глобальною/опцією командного рядка (src/parser.rs:165)
- [ ] Додати опцію ігнорування "сміття" після `msgstr` (src/parser.rs:512)

## Команди та фільтрація
- [ ] **Словники (Dictionaries)**: Якщо повідомлення містить слово зі словника, додати його як підказку для AI (src/main.rs:136)
- [ ] **Розширене сортування**: Сортування за розміром, за `msgstr`, за першим символом, за спеціальним символом тощо (src/main.rs:137)
- [ ] **Фільтрація**: Додати фільтр "без слів" (`without words`) (src/main.rs:141)

## Валідація та перевірки
- [ ] **Spaces Check**: Перевірка пробілів на початку/в кінці `msgstr` відповідно до `msgid` (src/main.rs:139)
- [ ] **Capital Letter Check**: Перевірка великої літери на початку `msgstr` відповідно до `msgid` (src/main.rs:140)
- [ ] **Spelling**: Перевірка орфографії (src/main.rs:144)

## AI Інтеграція
- [x] Абстрагування виклику AI-команд (структура `AiBackend`, підтримка `--ai-command`)
- [ ] **Auto-Fix**: Спроба автоматичного виправлення повідомлень після виявлення проблем при перекладі або рецензуванні (src/main.rs:142)
- [ ] Завершення реалізації команд `translate`, `review`, `filter` (WIP)
